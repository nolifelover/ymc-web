<!-- Main component for a primary marketing message or call to action -->
<div class="jumbotron">
	<h2>การแข่งขันคณิตศาสตร์ระหว่างโรงเรียน ครั้งที่ 1 ประจำปีการศึกษา 2556
		<br/>ชิงถ้วยรางวัล ฯพณฯ พลเอกเปรม ติณสูลานนท์ ประธานองคมนตรีและรัฐบุรุษ
	</h2>
	<p></p>
	<h4>ค้นหาบัตรประจำตัวผู้เข้าสอบ</h4>
		
	<form action="/seat/search" class="form-horizontal" role="form" id="search">
		<div class="form-group">
			<label for="firstname" class="col-sm-2 control-label">ชื่อ</label>
			<div class="col-sm-10">
				<input type="text" name="firstname" class="form-control" id="firstname" placeholder="ชื่อ">
			</div>
		</div>
		<div class="form-group">
			<label for="lastname" class="col-sm-2 control-label">นามสกุล</label>
			<div class="col-sm-10">
				<input type="text" name="lastname" class="form-control" id="lastname" placeholder="นามสกุล">
			</div>
		</div>
		<div class="form-group">
			<label for="code" class="col-sm-2 control-label">เลขประจำตัวสอบ</label>
			<div class="col-sm-10">
				<input type="text" name="code" class="form-control" id="code" placeholder="เลขประจำตัวสอบ">
			</div>
		</div>
		<div class="form-group">
			<label for="school" class="col-sm-2 control-label">โรงเรียน</label>
			<div class="col-sm-10">
				<input type="text" name="school" class="form-control" id="school" placeholder="โรงเรียน">
			</div>
		</div>
		<div class="form-group">
			<div class="col-sm-offset-2 col-sm-10">
				<button type="submit" class="btn btn-info">ค้นหา</button>
			</div>
		</div>
	</form>
</div>
<div class="row">
	<div id="search_result">

	</div>
</div>

<script type="text/javascript">
$("#search").submit(function(e)
{
	var postData = $(this).serializeArray();
	var formURL = $(this).attr("action");
	$.ajax({
		url : formURL,
		type: "POST",
		data : postData,
		success:function(data, textStatus, jqXHR){
			//data: return data from server
			$("#search_result").html(data);
    },
    error: function(jqXHR) 
    {
    	//console.log(jqXHR);
    	$("#search_result").html(jqXHR);
    }
  });
  e.preventDefault(); //STOP default action
});

//$("#search").submit(); //Submit  the FORM
</script>